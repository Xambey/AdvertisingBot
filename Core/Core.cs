using Core.Options;

namespace Core
{
    public class Core
    {
        private readonly AuthOptions _authOptions;
        
        public Core(AuthOptions authOptions)
        {
            _authOptions = authOptions;
        }

        public void Run()
        {
            
        }
        
    }
}